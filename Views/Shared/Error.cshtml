@model ErrorViewModel
<div class="spy-bg d-flex flex-column align-items-center justify-content-center" style="min-height:40vh;">
    <h1 class="spy-title mb-3">Bir Hata Oluştu</h1>
    <p class="text-light">@Model?.RequestId != null ? $"Hata Kodu: {Model.RequestId}" : "Beklenmeyen bir hata oluştu."</p>
    <a href="/" class="btn btn-danger mt-3">Ana Sayfa</a>
</div>
